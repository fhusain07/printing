import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <Link className="text-xl font-extrabold text-primary tracking-wide" to="/">
      SHIKSHABYTECH
    </Link>
  );
};

export default Logo;
